cstruct SDL_Event {
    type as uint32,
    timestamp as uint64
}

// Direct test - allocate and access without SDL
event := c.malloc(192) as SDL_Event

// Just access the field - should read whatever is there
value := event.type
println(value)
