import sdl3 as sdl

ptr := malloc(100)
printf("malloc returned: %p\n", ptr as int64)

ptr {
    0 => {
        println("malloc returned NULL!")
    }
    ~> {
        printf("malloc succeeded, freeing...\n")
        free(ptr)
    }
}
