import sdl3 as sdl

cstruct SDL_Event {
    type as uint32,
    timestamp as uint64
}

// Test c.malloc
ptr := c.malloc(192) as SDL_Event
ptr {
    0 => {
        println("malloc returned NULL")
    }
    ~> {
        println("malloc succeeded")
    }
}
