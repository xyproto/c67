import libc as c

// Test peek32
ptr = c.malloc(16)

// Write some test data
// (We can't write directly, but let's test reading)

val = peek32(ptr, 0)
println("Read value:")
println(val)

c.free(ptr)
println("Done!")
