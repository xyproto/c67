import sdl3 as sdl

cstruct SDL_Event {
    type as uint32,
    timestamp as uint64
}

event := c.malloc(192) as SDL_Event
println("Event allocated")

// Try to access field
event.type {
    0 => {
        println("type is 0")
    }
    ~> {
        println("type is non-zero")
    }
}
