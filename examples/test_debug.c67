cstruct SDL_Event {
    type as uint32
}

// This should set varTypes["event"] = "SDL_Event"
event := c.malloc(192) as SDL_Event

// Print the pointer value to make sure malloc worked
event {
    0 => {
        println("event is NULL")
    }
    ~> {
        println("event is not NULL")
    }
}

